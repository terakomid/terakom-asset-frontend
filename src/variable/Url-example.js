// const baseUrl = `http://localhost:8000`; // development
const baseUrl = `http://api-asset.terakom.id`; // production
const apiUrl = `${baseUrl}/api`;
const title = ` - Asset`;

export { baseUrl, apiUrl, title };
